version: 2
updates:
  # update-cask.yml pins its actions to commit SHAs, which never move on their
  # own. This is what tells us when a pinned action has a newer release.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
